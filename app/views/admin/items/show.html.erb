<div class="container">
  <div class="row mt-4">
    <div class="col-md-4">
      </div>
  </div>
  </div>
  <h2 class="text-center">商品詳細</h2>
  <div class="row mt-5">
    <div class="col-md-4 mx-auto">
       <h2>画像</h2>
<% if @item.image.attached? %>
  <%= image_tag @item.image, size: "300x300" %>
<% else %>
  <%= image_tag 'cake1.jpg', size: "300x300" %>
<% end %>
       
        </div>
      <div class="col-md-8">
     <table class="table table-striped">
        <tr>
          <th>商品名</th>
          <td><%= @item.name%></td></tr>
       <tr><th>商品説明</th>
       <td><%= @item.description %></td></tr> 
       <tr> <th>ジャンル</th>
        <td><%= @item.genre_id %></td></tr> 
       <tr> <th>税抜き価格</th>
        <td><%= @item.price %></td></tr> 
       <tr> <th>販売ステータス</th>
        <td></td></tr> 
       
  </table>
  </div>
   </div>
    <div class="text-center mt-3">
   <%= link_to "編集", edit_admin_item_path(@item.id) %>
    </div>
    </div>
    
     </div>
     
      